# .mjs 스크립트는 항상 LF로 체크아웃한다. Windows core.autocrlf=true가 CRLF로 바꾸면
# vitest(vite/esbuild)가 동적 import 시 "SyntaxError: Invalid or unexpected token"을 낸다
# (node 직접 실행은 무해). repo 저장은 이미 LF — 체크아웃 EOL만 고정한다.
*.mjs text eol=lf
