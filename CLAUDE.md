# Classin Home — Claude Code 지침

> 저장소 지침의 단일 소스(SSOT)는 [`AGENTS.md`](AGENTS.md)다. Claude Code는 AGENTS.md를 자동 로드하지 않으므로 아래 import로 전체 지침을 불러온다.
> 규칙 추가·수정은 항상 `AGENTS.md`에서만 하고 이 파일에는 중복하지 않는다. (Claude Code 전용 지침이 생기면 이 import 아래에 덧붙인다.)

@AGENTS.md
